# Bunny Video Recovery Upload Script

Backend-only Node.js script for recovering videos from existing CDN URLs and uploading them into a Bunny Stream Video Library.

The script reads a CSV with these columns:

```text
Video ID,Video Title,Status,Stream URL
